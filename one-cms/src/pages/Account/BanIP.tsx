import { Heading } from '@chakra-ui/react';
import { withTitle } from '@/utils/page';

export const BanIP = withTitle('navigation.account.banIp', () => {
  return (
    <div>
      <Heading>BanIP</Heading>
    </div>
  );
});

export default BanIP;

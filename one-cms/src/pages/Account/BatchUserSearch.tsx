import { Heading } from '@chakra-ui/react';
import { withTitle } from '@/utils/page';

export const BatchUserSearch = withTitle('navigation.account.batchUserSearch', () => {
  return (
    <div>
      <Heading>BatchUserSearch</Heading>
    </div>
  );
});

export default BatchUserSearch;

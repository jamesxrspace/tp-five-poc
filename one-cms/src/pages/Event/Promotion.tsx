import { Heading } from '@chakra-ui/react';
import { withTitle } from '@/utils/page';

export const Promotion = withTitle('navigation.event.promotion', () => {
  return (
    <div>
      <Heading>Promotion</Heading>
    </div>
  );
});

export default Promotion;
